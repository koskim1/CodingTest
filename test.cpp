#include <bits/stdc++.h>
using namespace std;

int a, b, c;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

    a = 2;
    cout << a << " : " << b << " : " << c << "\n";
    return 0;
}