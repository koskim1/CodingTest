#include <bits/stdc++.h>
using namespace std;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

/*
항상 변수가 초기화가 되어있나 확인해야 합니다.

ex) 문제에서 테스트케이스가 여러개 주어지고 주어진 테스트케이스 당... 하는 문제가 있습니다.

그럴 경우 보통 다음과 같은 코드 그리고 while 루프 안에서 로직이 이어질텐데이 안에서 변수 초기화가 잘 되었는지 항상 확인합시다.

cin >> t;
while(t-);
//로직

*/
}
