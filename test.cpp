#include <bits/stdc++.h>
using namespace std;

int main(void) {
    ios::sync_with_stdio(0);
    cin.tie(0);

/*
스택오버플로 (stack overflow)는 함수의 호출이 무한히 반복되면 발생합니다. 
함수가 무한적으로 호출하나 확인해야 합니다. 
함수는 스택에 쌓이면서 실행됩니다. 

스택의 모든 공간을 다 차지하고 난 후 더 이상의 여유 공간이 없을 때 또 다시 스택 프레임을 저장하게 되면,
해당 데이터는 스택 영역을 넘어가서 저장되게 되며 이러한 에러가 발생됩니다.
*/
}
